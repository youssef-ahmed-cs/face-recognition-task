[phases.setup]
nixPkgs = ["python310", "gcc", "cmake", "openblas", "liblapack"]
[phases.install]
cmds = [
  "python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install --upgrade pip setuptools wheel && pip install -r requirements.txt"
]

[variables]
NIXPACKS_PYTHON_VERSION = "3.10"